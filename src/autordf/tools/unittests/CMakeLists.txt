INCLUDE_DIRECTORIES("${CMAKE_CURRENT_BINARY_DIR}")

ADD_EXECUTABLE(
    testcodegen
    TestCodeGen.cpp
    "${CMAKE_CURRENT_BINARY_DIR}/foaf/foaf.h"
)

ADD_CUSTOM_COMMAND(
    OUTPUT  "${CMAKE_CURRENT_BINARY_DIR}/foaf/foaf.h"
    COMMAND "${CMAKE_CURRENT_BINARY_DIR}/../autordfcodegen" "${CMAKE_CURRENT_SOURCE_DIR}/foaf_20140114.rdf"
    DEPENDS "${CMAKE_CURRENT_BINARY_DIR}/../autordfcodegen" foaf_20140114.rdf
)
